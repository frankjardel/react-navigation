import React from 'react';
import {StyleSheet, Text} from 'react-native';

export default function About() {
    return <Text style={styles.text}>Learning React</Text>
}

const styles = StyleSheet.create({
    text: {
        margin: 17
    }
})
